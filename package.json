{
  "name": "bprcode-pages",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "host": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-for-deploy": "pwsh -c \"rm assets -r -force; rm index.html; tsc && vite build; mv dist/index-source.html index.html; mv dist/assets assets; rm dist;\""
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
